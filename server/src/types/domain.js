const roles = ['USER', 'ADMIN'];
const taskStatuses = ['TODO', 'IN_PROGRESS', 'DONE'];
const taskPriorities = ['LOW', 'MEDIUM', 'HIGH'];

module.exports = {
  roles,
  taskStatuses,
  taskPriorities,
  Role: {
    USER: 'USER',
    ADMIN: 'ADMIN'
  }
};